CREATE TABLE gersonvillugas.aire (estacion text ,titulo text ,latitud text,longitud text,fecha text,periodo text,
so2 text, no text, no2 text, co text, pm10 text, o3 text, dd text, vv text, tmp text, hr text, 
prb text, rs text, ll text, ben text, tol text, mxil text, pm25 text, primary key (estacion, fecha,
periodo));

select  * from gersonvillugas.aire;
